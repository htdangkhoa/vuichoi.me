import { StyleSheet } from "react-native";

export default StyleSheet.create({
  placeContainer: {
    position: "absolute",
    top: 0,
    width: "100%",
    flex: 1,
    padding: 8
  },
  btnNext: {
    borderRadius: 8,
    position: "absolute",
    bottom: 32,
    left: 16,
    right: 16
  }
});
